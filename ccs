/* Estilos para o contêiner do mapa */
#map {
    width: 100%;
    height: 100%; /* O mapa deve ocupar toda a página */
    page-break-after: always; /* Quebra de página após cada mapa ao imprimir */
}

/* Estilos para o contêiner do popup */
.leaflet-popup-content-wrapper {
    background-color: #ffffff;
    border: 1px solid #ccc;
    border-radius: 5px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    font-family: Arial, sans-serif;
    font-size: 12px;
    line-height: 1.4;
}

/* Estilos para o conteúdo do popup */
.leaflet-popup-content {
    padding: 8px;
    color: #333333;
}

/* Estilos para os controles do mapa */
.leaflet-control-zoom {
    background-color: #ffffff;
    border: 1px solid #ccc;
    border-radius: 4px;
}

/* Estilos para os marcadores */
.leaflet-marker-icon {
    background-color: #ff0000;
    border-radius: 50%;
    width: 10px;
    height: 10px;
}
